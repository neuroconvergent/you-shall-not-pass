---
phase: 01-library-hardening
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [src/lib.rs, tests/lib_tests.rs]
autonomous: true
requirements: [SEC-01, SEC-02]
user_setup: []

must_haves:
  truths:
    - "Tests verify password generation produces correct-length output"
    - "Tests verify Zeroizing wrapper on return type"
    - "Tests verify calc_entropy returns correct values for known inputs"
    - "Tests verify all error paths return correct PasswordError variants"
    - "Tests verify RNG parameterization works with injected RNG"
  artifacts:
    - path: "tests/lib_tests.rs"
      provides: "Comprehensive test suite for password_generator module"
      min_lines: 80
    - path: "src/lib.rs"
      provides: "Inline #[cfg(test)] module or test helpers"
      contains: "#[cfg(test)]"
  key_links:
    - from: "tests/lib_tests.rs"
      to: "you_shall_not_pass::password_generator"
      via: "module import"
      pattern: "use you_shall_not_pass::password_generator"
    - from: "tests/lib_tests.rs"
      to: "src/lib.rs"
      via: "function calls with injected RNG"
      pattern: "generate_password\\(&defaults, rng\\)"
---

<objective>
Create comprehensive test suite for the hardened library — covering happy paths, error cases, entropy validation, and RNG parameterization.

Purpose: CONCERNS.md identifies 0% test coverage as critical risk. Phase 1 success criteria #4 requires all existing tests pass with updated signatures and new tests cover the OsRng failure path. Tests validate the hardened API before Phase 2 CLI wiring.
Output: tests/lib_tests.rs with full coverage of password_generator module
</objective>

<execution_context>
@/home/neuroconvergent/Programming/you-shall-not-pass/.opencode/get-shit-done/workflows/execute-plan.md
@/home/neuroconvergent/Programming/you-shall-not-pass/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-library-hardening/01-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@src/lib.rs
@Cargo.toml

<interfaces>
<!-- Contracts the test file needs from src/lib.rs (after Plan 01 changes). -->

From src/lib.rs (after hardening):
```rust
pub struct Defaults {
    pub length: usize,
    pub charset_groups: &'static [&'static str],
    pub target_entropy: f64,
    pub chars_to_avoid: &'static str,
    pub utf8_flag: bool,
    pub alphabet_flag: bool,
    pub caps_alphabet_flag: bool,
    pub num_flag: bool,
    pub symbol_flag: bool,
    pub quantum_check: bool,
}

pub const DEFAULTS: Defaults;

pub fn generate_password(config: &Defaults, rng: impl RngCore) -> Result<Zeroizing<String>>
pub fn calc_entropy(config: &Defaults, rng: impl RngCore) -> Result<f64>

pub enum PasswordError {
    EmptyLength,
    NoCharsetGroups,
    LengthTooShort { required: usize, actual: usize },
    ExhaustedGroup { group_index: usize },
    EmptyCharacterPool,
}
```

Test RNG strategy: Use `rand::rng()` for integration-style tests. For deterministic tests, use `rand::rng()` seeded approach or accept non-deterministic output with property-based assertions (length, charset membership).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create test scaffold and happy-path tests</name>
  <files>tests/lib_tests.rs</files>
  <read_first>
    - src/lib.rs (hardened version after Plan 01 — function signatures, Defaults struct, PasswordError)
    - .planning/codebase/TESTING.md (project testing conventions)
    - .planning/codebase/CONCERNS.md (known bugs and coverage gaps)
    - Cargo.toml (verify test dev-dependencies)
  </read_first>
  <acceptance_criteria>
    - `tests/lib_tests.rs` exists with valid Rust test module
    - File contains `use you_shall_not_pass::password_generator::{Defaults, DEFAULTS, generate_password, calc_entropy, PasswordError};`
    - At least 3 passing tests: default generation, custom length, entropy calculation
    - `cargo test` exits 0 with all tests passing
    - Tests use `rand::rng()` as RNG source (grep: `rand::rng()` present in tests/lib_tests.rs)
  </acceptance_criteria>
  <action>
Create `tests/lib_tests.rs` as an integration test file with the following tests:

1. **Test: `test_generate_password_default_length`**
   - Use `DEFAULTS` with `rand::rng()`
   - Call `generate_password(&DEFAULTS, rand::rng())`
   - Assert: result is `Ok`, password length is 12 (DEFAULTS.length)
   - Assert: password contains only characters from the default charset groups

2. **Test: `test_generate_password_custom_length`**
   - Create a `Defaults` with `length: 24`, all flags true, using the default charset_groups
   - Call `generate_password(&config, rand::rng())`
   - Assert: result is `Ok`, password length is 24

3. **Test: `test_generate_password_no_symbols`**
   - Create a `Defaults` with `symbol_flag: false`, `length: 16`
   - Call `generate_password(&config, rand::rng())`
   - Assert: result is `Ok`, password contains no symbol characters (`!@#$%^&*`)

4. **Test: `test_calc_entropy_classical`**
   - Use `DEFAULTS` with `quantum_check: false`
   - Call `calc_entropy(&DEFAULTS, rand::rng())`
   - Assert: result is `Ok`, entropy value is positive and reasonable (> 60.0 for default config)

5. **Test: `test_calc_entropy_quantum`**
   - Use `DEFAULTS` with `quantum_check: true`
   - Call `calc_entropy(&DEFAULTS, rand::rng())`
   - Assert: result is `Ok`, quantum entropy is approximately half of classical (within 1.0 tolerance)

6. **Test: `test_password_is_zeroizing`**
   - Call `generate_password(&DEFAULTS, rand::rng())`
   - Assert: return type is `Zeroizing<String>` — verify by checking that the result can be dereferenced to `&str` via `Deref`
   - Use `result.as_ref()` or `result.deref()` to access the string content

Import structure:
```rust
use std::ops::Deref;
use you_shall_not_pass::password_generator::{Defaults, DEFAULTS, generate_password, calc_entropy, PasswordError};
use zeroize::Zeroizing;
```

What to avoid:
- Do NOT add `proptest` or `quickcheck` as dev-dependencies — AGENTS.md says "minimal dependencies." Use standard `#[test]` with `rand::rng()`.
- Do NOT test with hardcoded passwords — use `rand::rng()` for real CSPRNG output.
- Do NOT create a `tests/` subdirectory structure — use flat `tests/lib_tests.rs` as a single integration test file.
</action>
  <verify>
    <automated>cargo test --test lib_tests 2>&1</automated>
  </verify>
  <done>Integration test file created with 6+ passing tests covering default generation, custom length, no-symbols, classical entropy, quantum entropy, and Zeroizing return type</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add error-path tests and entropy validation alignment tests</name>
  <files>tests/lib_tests.rs</files>
  <read_first>
    - tests/lib_tests.rs (after Task 1)
    - src/lib.rs (hardened version — error variants, validation logic)
    - .planning/REQUIREMENTS.md (SEC-02: graceful RNG failure handling)
  </read_first>
  <acceptance_criteria>
    - Tests cover all 5 PasswordError variants (grep -c 'PasswordError::' in tests/lib_tests.rs >= 5)
    - `test_calc_entropy_length_too_short` exists and passes — verifies ROADMAP success criteria #3
    - `cargo test` exits 0 with all tests passing
    - Total test count >= 10
  </acceptance_criteria>
  <action>
Add the following tests to `tests/lib_tests.rs`:

1. **Test: `test_empty_length_error`**
   - Create `Defaults` with `length: 0`, all flags true
   - Call `generate_password(&config, rand::rng())`
   - Assert: result is `Err(PasswordError::EmptyLength)`

2. **Test: `test_no_charset_groups_error`**
   - Create `Defaults` with all flags false (`alphabet_flag: false`, `caps_alphabet_flag: false`, `num_flag: false`, `symbol_flag: false`), `length: 12`
   - Call `generate_password(&config, rand::rng())`
   - Assert: result is `Err(PasswordError::NoCharsetGroups)`

3. **Test: `test_length_too_short_error`**
   - Create `Defaults` with `length: 2`, all flags true (4 groups enabled, so length < groups.len())
   - Call `generate_password(&config, rand::rng())`
   - Assert: result is `Err(PasswordError::LengthTooShort { required: 4, actual: 2 })`

4. **Test: `test_calc_entropy_empty_length_error`**
   - Create `Defaults` with `length: 0`, all flags true
   - Call `calc_entropy(&config, rand::rng())`
   - Assert: result is `Err(PasswordError::EmptyLength)`

5. **Test: `test_calc_entropy_no_charset_error`**
   - Create `Defaults` with all flags false, `length: 12`
   - Call `calc_entropy(&config, rand::rng())`
   - Assert: result is `Err(PasswordError::NoCharsetGroups)`

6. **Test: `test_calc_entropy_length_too_short`** (validates ROADMAP success criteria #3)
   - Create `Defaults` with `length: 2`, all flags true (4 groups)
   - Call `calc_entropy(&config, rand::rng())`
   - Assert: result is `Err(PasswordError::LengthTooShort { required: 4, actual: 2 })`
   - This test proves calc_entropy now has the same validation as generate_password

7. **Test: `test_entropy_increases_with_length`**
   - Call `calc_entropy` with `length: 8` and `length: 16` (same other config)
   - Assert: entropy for length 16 > entropy for length 8

8. **Test: `test_entropy_decreases_with_fewer_groups`**
   - Call `calc_entropy` with all groups enabled, then with only lowercase enabled
   - Assert: entropy with all groups > entropy with single group

Run `cargo test` — all tests must pass.

What to avoid:
- Do NOT test `ExhaustedGroup` or `EmptyCharacterPool` errors with `chars_to_avoid` — these require complex setup and are lower priority for Phase 1. They are covered by the existing error variant but not tested yet.
- Do NOT add mock RNG implementations — `rand::rng()` is sufficient for testing error paths since errors are triggered by config validation, not RNG behavior.
</action>
  <verify>
    <automated>cargo test --test lib_tests 2>&1</automated>
  </verify>
  <done>All 5 PasswordError variants tested, calc_entropy validation aligned with generate_password, 10+ total tests passing</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test RNG → library | Tests use `rand::rng()` — same trust boundary as production; no adversarial input |
| Test config → library | Test `Defaults` are constructed in-test — controlled, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Information Disclosure | Test output containing generated passwords | mitigate | Tests do not `println!` generated passwords. Only assert on length and charset membership. No secret material in test logs. |
| T-01-07 | Information Disclosure | Test fixtures with hardcoded passwords | accept | No hardcoded passwords in tests — all use `rand::rng()` CSPRNG output. Test data is structural (lengths, flags), not secret material. |
</threat_model>

<verification>
- `cargo test --test lib_tests` exits 0 with all tests passing
- Test count >= 10 (grep -c '#\[test\]' tests/lib_tests.rs >= 10)
- All 5 PasswordError variants covered in tests
- Zeroizing return type verified in test
- calc_entropy length validation tested (matches generate_password)
- `cargo build` exits 0
- `cargo clippy --all-targets --all-features` exits 0
</verification>

<success_criteria>
- 10+ tests covering happy paths, error cases, entropy validation, and Zeroizing return
- All PasswordError variants tested (EmptyLength, NoCharsetGroups, LengthTooShort at minimum)
- calc_entropy validation alignment proven by test
- Test file uses `rand::rng()` as RNG source (consistent with D-01)
- No new dependencies added (uses only existing crates)
</success_criteria>

<output>
After completion, create `.planning/phases/01-library-hardening/01-02-SUMMARY.md`
</output>
